{
  "name": "homehold",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:db": "docker compose up -d homehold-db",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npm run dev:db && concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
