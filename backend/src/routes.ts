// Route aggregator, mirroring eneri-be's pattern: each app module's router
// gets imported and re-exported here, then mounted in server.ts.
//
// No entity routes yet — add them as pet-project / auth features land.
