# Copy to .env before running `docker compose up`. Docker Compose reads a
# root .env automatically for variable substitution in docker-compose.yml.
POSTGRES_USER=homehold
POSTGRES_PASSWORD=change-me
POSTGRES_DB=homehold
